import { PublicationInterceptor } from './publication.interceptor';

describe('PublicationInterceptor', () => {
  it('should be defined', () => {
    expect(new PublicationInterceptor()).toBeDefined();
  });
});
